{
  "name": "syssim-clone",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace web dev",
    "build": "yarn workspace web build",
    "build:wasm": "yarn workspace sim-engine build",
    "build:all": "yarn build:wasm && yarn build"
  },
  "packageManager": "yarn@1.22.22"
}
